import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ForgotPassword04Page } from './forgot-password04.page';

describe('ForgotPassword04Page', () => {
  let component: ForgotPassword04Page;
  let fixture: ComponentFixture<ForgotPassword04Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ForgotPassword04Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
