import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VideoCartoon002Page } from './video-cartoon002.page';

describe('VideoCartoon002Page', () => {
  let component: VideoCartoon002Page;
  let fixture: ComponentFixture<VideoCartoon002Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(VideoCartoon002Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
