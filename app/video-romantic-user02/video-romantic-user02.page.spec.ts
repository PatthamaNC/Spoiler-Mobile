import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VideoRomanticUser02Page } from './video-romantic-user02.page';

describe('VideoRomanticUser02Page', () => {
  let component: VideoRomanticUser02Page;
  let fixture: ComponentFixture<VideoRomanticUser02Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(VideoRomanticUser02Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
