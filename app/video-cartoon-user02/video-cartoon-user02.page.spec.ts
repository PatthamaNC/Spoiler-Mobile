import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VideoCartoonUser02Page } from './video-cartoon-user02.page';

describe('VideoCartoonUser02Page', () => {
  let component: VideoCartoonUser02Page;
  let fixture: ComponentFixture<VideoCartoonUser02Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(VideoCartoonUser02Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
