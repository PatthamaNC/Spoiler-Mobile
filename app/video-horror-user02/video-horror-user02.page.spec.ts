import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VideoHorrorUser02Page } from './video-horror-user02.page';

describe('VideoHorrorUser02Page', () => {
  let component: VideoHorrorUser02Page;
  let fixture: ComponentFixture<VideoHorrorUser02Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(VideoHorrorUser02Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
